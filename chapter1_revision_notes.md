# 第一章 `1.1-1.2` 修改思路说明

## 总体判断

这次对第一章的修改，不是简单润色，而是对绪论的叙事重心、文献组织方式和方法展示密度进行重新校准。核心目标有三点：

1. 让 `1.1` 更像“背景立题”，而不是泛化的智慧交通背景介绍。
2. 让 `1.2` 更像“方法综述”，而不是句尾连引或文献堆砌。
3. 保持全文主线稳定，即：

`连续三轴地磁观测 -> 统一事件级输入 -> 车型三分类 / 停车与占用判定`

也就是说，第一章不能把第二章写成与第三章、第四章并列的研究重点，而应把第二章压回到“共享输入接口”和“系统支撑”这一定位。

此外，本轮修改新增了一个最高优先级约束：

- 所有文献表述必须严格以本地 `references.bib`、`tmp/pdfs` 和现有正文中可回溯的信息为准。
- 不补造作者结论，不补造准确率，不补造实验结果。
- 没有本地可核实结果的文献，只写方法或场景作用，不强行写数字。

## 本轮对照师兄师姐后的额外校准

在向倪竺和柳文崇的对应小节中，方法综述的落法有两个非常稳定的共同点：

1. 方法句通常采用“作者名后直接挂编号”的写法，例如“Balid 等人[28]提出……”“Zhu 等人[26]提出……”，而不是“Balid 等人提出……[28]”。
2. 展示重点不是单纯说“做了什么”，而是尽量落到“作者 + 方法 + 关键效果/结果”这一层；没有合适结果时，也至少要把方法做法说清楚。

因此，这一轮对 `1.2.1-1.2.3` 又额外做了一次统一校准：

- 作者引导式的方法句，参考文献统一尽量挂在作者名后面。
- 优先保留一篇文献对应一个方法判断，减少句尾大串连引。
- 能从本地材料核实的准确率、误差和检测效果，尽量保留；不能核实的只写方法，不补数字。

## `1.1 研究背景及研究意义` 修改思路

### 1.1 的任务定位

`1.1` 的任务不是介绍具体方法，而是完成“从宏观背景到研究问题”的收束。对应你这篇论文，最终应该落到两个上层任务：

- 单地磁条件下的车型三分类
- 开放道路场景下的停车与占用判定

第二章所对应的“车辆事件检测”和“事件级输入构造”在 `1.1` 中只能作为前提，不宜被写成独立主角。

### 1.1 的结构安排

最终采用的是五段式结构：

1. 城市交通压力与智慧交通背景
2. 为什么道路侧单地磁值得研究
3. 为什么车型信息重要，以及单地磁分类的难点
4. 为什么停车/占用状态重要，以及开放道路场景的难点
5. 全文研究落点

这种结构的好处是：

- 保留了向倪竺式的“背景 -> 任务”收束节奏
- 让两个核心任务在 `1.1` 中被提前亮出来
- 不会让第二章抢掉第三章、第四章的存在感

### 1.1 的文献组织原则

这次 `1.1` 不再只依赖少量综述文献支撑整节，而是按“文献角色”重新分配：

- 报告/政策类：支撑城市交通压力和政策牵引
- ITS/智慧道路类：支撑智慧交通与路侧感知的技术背景
- 低成本感知技术类：支撑单地磁的适用性
- 单磁感知任务类：支撑车型信息和停车/占用信息的任务意义

同时，尽量避免以下问题：

- 一句话堆多篇文献
- 用同一篇综述文献在多个相邻判断中反复支撑
- 用“已有研究表明”“已有综述表明”这种虚主语写法代替真实作者落句

例如，原来 `1.1` 中“已有综述表明……”这类句子过于泛化，因此改成了：

- `Mokaddem 和 Jawab 对城市智能交通系统研究与应用的系统综述指出……`

这样做的好处是：

- 作者清楚
- 文献角色明确
- 句子更像学位论文里的文献落法，而不是概括式口吻

## `1.2 国内外研究现状` 修改思路

### 1.2 的总体定位

`1.2` 的任务不是重复 `1.1` 的背景，也不是提前替全文下结论，而是对后文方法问题进行分块综述。最终保留三个维度：

1. 单地磁车辆事件检测
2. 单地磁车型分类
3. 单地磁停车与占用检测

这三节的功能不是并列地宣称三项研究贡献，而是对应全文主链路中的三个层级：

- `1.2.1`：共享输入前提
- `1.2.2`：事件级识别任务
- `1.2.3`：状态级判定任务

因此，`1.2` 开头只保留导语功能，不再提前做整节总结，更不在这里展开研究缺口。

### 1.2 的统一写法原则

这次 `1.2` 的改写遵循四条统一规则：

1. 优先写成“作者 + 方法 + 结果/作用 + 局限/启发”
2. 单句默认只承载 1 篇方法文献
3. 结果指标采用“每段 1-2 个”的密度，不追求篇篇写数字
4. 没有本地可核实结果的文献，只写方法内容，不补造指标

这也是为什么 `1.2` 从上一版的“概括式综述”被收成现在这种“展示型综述”的原因。

## `1.2.1 单地磁车辆事件检测研究现状` 修改思路

### 这一节为什么保留

虽然向倪竺没有单独把检测综述写成一大节，但对你这篇论文来说，`1.2.1` 仍然需要保留。原因是：

- 你的第二章承担的是统一事件级输入接口作用
- 第三章和第四章都建立在事件级输入之上
- 如果在 `1.2` 中完全跳过事件检测这一层，后面的两节会像直接从连续信号上长出来，链路会变松

### 这一节为什么要短

`1.2.1` 不是正文主战场，不能扩成与 `1.2.2`、`1.2.3` 并列的大综述。它的功能只有两个：

1. 交代单地磁车辆事件检测已有代表性工作
2. 说明事件检测对本文的意义在于形成统一事件级输入

因此这里只保留 2-3 篇代表性检测方法文献，用作者引导方式落句，不再写长串并列 `\cite{...}`。

## `1.2.2 单地磁车型分类研究现状` 修改思路

### 结构安排

这一节保留五段结构：

1. 单地磁分类的可行性
2. 任务设定与样本口径差异
3. 特征工程路线
4. 时序建模路线
5. 研究空白与本文切入点

这五段结构的作用，是把这节从“逐篇列文献”改成“围绕第三章真实问题的综述”。

### 为什么这样写

你的第三章不是泛泛做分类，而是很明确地分成两条路线：

- 端侧轻量统计特征路线
- 面向时间尺度变化的时序增强路线

因此，`1.2.2` 不能只写“已有很多分类方法”，而必须把已有工作组织成：

- 特征工程方法
- 时序/深度学习方法

并且在结尾明确收束到第三章的主问题：

- 自然车流条件下的时间尺度变化
- 局部结构错位
- 这两者对固定统计量和简单定长表示的破坏

### 文献使用原则

这一节尽量选择本地能核实的代表方法：

- 可行性：`R17`、`R21`、`R22`
- 综合系统与结果：`R18`
- 特征工程：`R20`、`R23`、`ch3_zhang2019featureselection`
- 时序建模：`ch3_li2020singlemag`、`feng2022magmonitor`、`R26`

其中结果指标只保留本地能核实的：

- `Balid` 的四项准确率
- `Li 2020` 的 97.83% 车辆分类准确率和 96.85% 速度区间估计准确率

没有明确数字支撑的文献，例如部分可行性类和结构性方法文献，只保留方法或作用描述。

### 这一节额外保留的一层

按照你的论文主线，这节还必须保留“事件边界截取、样本长度组织、输入表示方式不一致”这一层。原因是这句话不是一般性综述补充，而是直接给第三章 `3.2.2` 的事件级样本与输入表示铺路。

## `1.2.3 单地磁停车与占用检测研究现状` 修改思路

### 这一节的核心定位

这一节不能写成“停车感知系统综述”，而要写成“开放道路停车与占用问题综述”。这是因为你的第四章真正研究的是：

- 停车确认
- 占用建立、维持与释放
- 连续车流、慢漂移、伪稳态条件下的状态判定一致性

因此，这一节的重点不是“停车系统有哪些”，而是：

- 停车检测与占用状态判定的区别
- 封闭停车位场景与开放道路场景的区别
- 现有规则方法和鲁棒性增强方法各自做到什么程度
- 为什么开放道路条件下还需要新的稳态证据与参考维护机制

### 这一节的结构安排

最终保留五步结构：

1. 任务区分：停车检测 vs 占用状态判定
2. 场景区分：封闭车位/停车场 vs 开放道路
3. 传统规则方法：阈值、差分、基线更新、抗干扰
4. 鲁棒性增强方法：有限状态机、协同决策、多源信息、协同跟踪
5. 开放道路核心困难：连续车流、慢漂移、伪稳态

### 为什么要再做一轮精修

上一轮改写后，`1.2.3` 的方向已经对了，但仍带有一点“场景铺垫偏重、方法展示偏后”的问题。因此这次又做了两点强化：

- 压缩前两段的泛综述和场景解释，让第二段更快进入方法与场景代表工作
- 增强第三、四段的方法展示，把代表方法的做法和可核实结果写得更具体

### 文献使用原则

这一节的方法文献主要围绕以下几篇展开：

- `ch4_zhang2015amr`
- `ch4_zhang2020railway`
- `R47`
- `R45`
- `ch4_zhang2020tracking`
- `R43`

场景/综述类文献只承担“任务与场景交代”功能：

- `R49`
- `R44`
- `ch4_lin2017surveyparking`
- `R52`
- `ch4_trigona2016smartparking`
- `ch4_gu2013streetparking`

结果指标只保留本地可以核实的：

- `R47`：进入/离开停车位检测准确率分别为 99.8% 和 99.9%
- `ch4_zhang2020tracking`：停车检测准确率达到 99.54%

其他文献如 `R45`、`R43`、`ch4_trigona2016smartparking` 没有在本地材料中找到可直接复述的统一指标，因此只保留方法和场景作用，不补造数字。

### 这一节最后为什么落到“稳态证据”

第四章真正的关键词不是“停车检测算法很多”，而是：

- 可靠稳态证据
- 参考量维护
- 停车确认与占用状态判定的一致性

所以 `1.2.3` 的最后必须自然落到这三个问题上，而不是停在“场景复杂、仍需进一步研究”这种空泛结论。

## 文献写法原则总结

这次 `1.1-1.2` 的文献写法统一遵循以下原则：

1. 优先作者引导式，不再大量使用“已有研究表明”。
2. 单句默认只写 1 篇文献，必要时最多 2 篇。
3. 不为追求“展示得厚”而补造结果。
4. 综述类文献负责交代背景和场景，方法类文献负责展示方法与结果。
5. 尽量减少同一篇文献在相邻段落中连续复用支撑多个核心判断。

## 当前可讨论结论

如果要给当前版本一个整体判断，可以概括为：

- `1.1` 已经从“背景较泛”收成了“围绕两类核心任务立题”的写法。
- `1.2.1` 已经回到短过渡节定位。
- `1.2.2` 已经基本形成稳定版本，主问题和第三章能直接对接。
- `1.2.3` 经过本轮再收后，已经更接近“开放道路停车与占用问题综述”，方法展示感比上一轮更强。

如果后续继续改，最值得再讨论的通常不是结构，而是：

- 个别句子的措辞强度是否还要再压稳
- `1.2.3` 前两段的场景铺垫是否还要再压缩半步
- `1.3` 是否要继续按这一套风格同步收紧
